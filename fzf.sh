# source the fzf configuration, which powers fuzzy complete and
# key-bindings and gives us the nice history search
# requires fzf to be installed
# https://github.com/junegunn/fzf
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
